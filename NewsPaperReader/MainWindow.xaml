<Window x:Class="NewsPaperReader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NewsPaperReader"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="在线报纸阅读器" Height="800" Width="1200"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <!-- 工具栏 -->
        <ToolBarTray DockPanel.Dock="Top" IsLocked="True">
            <ToolBar>
                <Button Content="添加报纸" Command="{Binding AddNewspaperCommand}" Style="{StaticResource DefaultButton}"/>
                <Button Content="刷新" Command="{Binding RefreshCommand}" Style="{StaticResource DefaultButton}"/>
                <Separator/>
                <Button Content="设置" Command="{Binding SettingsCommand}" Style="{StaticResource DefaultButton}"/>
                <Button Content="关于" Command="{Binding AboutCommand}" Style="{StaticResource DefaultButton}"/>
            </ToolBar>
        </ToolBarTray>
        
        <!-- 主内容区域 -->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="30"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/>
                    <ColumnDefinition Width="250"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <!-- 自动隐藏触发区域 -->
                <Border Grid.Column="0" Width="5" Background="Transparent" MouseEnter="AutoHideTrigger_MouseEnter"/>
                
                <!-- 报纸列表 -->
                <Border x:Name="NewspaperListBorder" Grid.Column="0" BorderBrush="LightGray" BorderThickness="0,1,1,0" MouseLeave="Sidebar_MouseLeave">
                    <DockPanel>
                        <TextBlock Text="报纸列表" FontWeight="Bold" DockPanel.Dock="Top" Style="{StaticResource DefaultTextBlock}"/>
                        <ListBox ItemsSource="{Binding Newspapers}" 
                                 SelectedItem="{Binding SelectedNewspaper}"
                                 BorderThickness="0">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="10"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                        <Image Source="{Binding TitleImagePath}" Width="50" Height="50" Stretch="Uniform" Margin="0,0,10,0"/>
                                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </DockPanel>
                </Border>
                
                <!-- 版面列表 -->
                <Border x:Name="EditionListBorder" Grid.Column="1" BorderBrush="LightGray" BorderThickness="0,1,1,0" MouseLeave="Sidebar_MouseLeave">
                    <DockPanel>
                        <TextBlock Text="版面列表" FontWeight="Bold" DockPanel.Dock="Top" Style="{StaticResource DefaultTextBlock}"/>
                        <ListBox ItemsSource="{Binding Editions}" 
                                 SelectedItem="{Binding SelectedEdition}"
                                 DisplayMemberPath="Title"
                                 BorderThickness="0">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="10"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </DockPanel>
                </Border>
                
                <!-- PDF查看器 -->
                <Border Grid.Column="2" BorderBrush="LightGray" BorderThickness="0,1,0,0">
                    <DockPanel>
                        <TextBlock Text="PDF查看器" FontWeight="Bold" DockPanel.Dock="Top" Style="{StaticResource DefaultTextBlock}"/>
                        <wv2:WebView2 x:Name="WebView2PdfViewer" Source="about:blank"/>
                    </DockPanel>
                </Border>
            </Grid>
            <!-- 状态栏 -->
            <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="0,1,0,0" Background="#F0F0F0">
                <TextBlock Text="{Binding StatusText}" Margin="10" VerticalAlignment="Center" Foreground="#333333"/>
            </Border>
        </Grid>
    </DockPanel>
</Window>
