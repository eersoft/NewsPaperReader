<Window x:Class="NewsPaperReader.AddNewspaperDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NewsPaperReader"
        mc:Ignorable="d"
        Title="添加报纸" Height="300" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="添加新报纸" FontWeight="Bold" FontSize="16" Grid.Row="0" Margin="10"/>
        
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="10">
            <TextBlock Text="报纸名称:" VerticalAlignment="Center" Width="80"/>
            <TextBox Text="{Binding NewspaperName}" Style="{StaticResource DefaultTextBox}"/>
        </StackPanel>
        
        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="10">
            <TextBlock Text="报纸URL:" VerticalAlignment="Center" Width="80"/>
            <TextBox Text="{Binding NewspaperUrl}" Style="{StaticResource DefaultTextBox}"/>
        </StackPanel>
        
        <StackPanel Grid.Row="3" Margin="10">
            <TextBlock Text="提示:" FontWeight="Bold" Margin="0,10,0,5"/>
            <TextBlock Text="1. 输入报纸的官方网站URL" FontSize="12" Foreground="Gray"/>
            <TextBlock Text="2. 系统将自动分析网页结构" FontSize="12" Foreground="Gray"/>
            <TextBlock Text="3. 请确保URL可以访问" FontSize="12" Foreground="Gray"/>
        </StackPanel>
        
        <StackPanel Orientation="Horizontal" Grid.Row="4" HorizontalAlignment="Right" Margin="10">
            <Button Content="取消" Click="CancelButton_Click" Style="{StaticResource DefaultButton}"/>
            <Button Content="确定" Click="OKButton_Click" Style="{StaticResource DefaultButton}"/>
        </StackPanel>
    </Grid>
</Window>